<h1>みんなの投稿</h1>

<h4>これまで、投稿した一覧がみれます<br>
写真から投稿の詳細を見ることができます</h4>

<% @posts.each  do |post| %>
<div class="container">
  <div class="row">
  <div class="col-lg-8">
  <div class="row">
  	<div class="col-lg-4">
  		<h1> <%= link_to post_path(post) do %>
        <%= attachment_image_tag post, :image, :fill,60,60,
                      format: 'jpeg', class:"img-circle pull-left profile-img", fallback: "no_image.jpg", size:'60x60' %><% end %>
      </h1>
  	</div>
  	<div class="col-lg-8">
  		<h4><%= post.title %></h4>
  		<h4><%= post.created_at.strftime('%Y/%m/%d') %></h4>
  		<h4><%= post.user.first_name %></h4>
    </div>
  </div>
  </div>
  <div class="col-lg-4" style="background-color: lightpink;">
    <h2>広告募集中</h2>
  </div>
</div>
</div>

<% end %>
<%= paginate @posts %>
